// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import type { AfframeClient } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

export function registerGetAccountingCorporateIncomeTax(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "get_accounting_corporate_income_tax",
    {
      title: "Get corporate income tax (DPPO)",
      description: `Computes the DPPO base and tax (§23/1 base, §25 non-deductibles, §34 loss carry-forward, rate, zálohy §38a) for the period. Organization-scoped.`,
      annotations: {
        ...defaultAnnotationsForMethod("get"),
        ...getAnnotations("getAccountingCorporateIncomeTax"),
      },
    },
    async (): Promise<CallToolResult> => {
      try {
        const { data, error, response } = await client.GET("/v1/accounting/periods/{periodId}/outputs/corporate-income-tax")
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
