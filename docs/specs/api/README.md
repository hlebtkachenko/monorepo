# API Specifications

Future public API contracts that are not implemented yet.

- [`SANDBOX.md`](SANDBOX.md): test keys, fixtures, and force-trigger endpoints
- [`WEBHOOKS.md`](WEBHOOKS.md): delivery, signature, retry, and event contract

Current API behavior belongs in [`docs/api/`](../../api/). Each concept file
must link its owning GitHub issue or initiative and must not be presented as a
live contract.
