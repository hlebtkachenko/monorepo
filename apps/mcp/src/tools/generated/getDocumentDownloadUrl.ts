// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, operations } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "id": z.string().uuid().describe("Document id, resolved within the API key's workspace."),
}

export function registerGetDocumentDownloadUrl(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "get_document_download_url",
    {
      title: "Get a document download URL",
      description: `Mints a short-lived presigned S3 URL for a document's bytes (attachment disposition). The caller fetches the bytes DIRECT from S3 — they never pass through the API. Returns 404 when the document is not visible to the caller's workspace or has been soft-deleted.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("get"),
        ...getAnnotations("getDocumentDownloadUrl"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const raw = args as Record<string, unknown>
        const params = { path: { "id": raw["id"] } } as unknown as NonNullable<operations["getDocumentDownloadUrl"]["parameters"]>
        const init = { params }
        const { data, error, response } = await client.GET("/v1/documents/{id}/download-url", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
