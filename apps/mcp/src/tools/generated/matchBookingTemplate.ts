// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, components } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "counterpartyKey": z.string().min(1).max(255).describe("Counterparty identity — an IČO or normalized name."),
  "direction": z.enum(["RECEIVED","ISSUED"]).describe("FP (RECEIVED) vs FV (ISSUED)."),
  "supplyKind": z.enum(["GOODS","MATERIAL","SERVICES","UTILITY","RENT","INSURANCE","ASSET","ADVANCE","CREDIT_NOTE","OTHER"]).describe("Kind of supply."),
  "jurisdiction": z.enum(["DOMESTIC","REVERSE_CHARGE","EU","IMPORT","EXEMPT","OUTSIDE_VAT"]).describe("VAT jurisdiction."),
}

export function registerMatchBookingTemplate(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "match_booking_template",
    {
      title: "Match a case signature against confirmed booking templates",
      description: `Pure read: given a recurring case's signature (counterparty/direction/supplyKind/jurisdiction), returns the workspace's matching CONFIRMED booking template, or null for a novel/unmatched case. No mutation, no write-tool call — a match only supplies input facts to the SAME typed write calls the Brain already makes after full reasoning; every write, templated or not, still runs through the unchanged gate and is still HELD at cold start.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("post"),
        ...getAnnotations("matchBookingTemplate"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const body = args as unknown as components["schemas"]["MatchBookingTemplateRequest"]
        const init = { body }
        const { data, error, response } = await client.POST("/v1/booking-templates/match", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
