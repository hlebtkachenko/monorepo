// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, operations } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "accountId": z.string().uuid().describe("Opaque account identifier (UUID)."),
}

export function registerGetAccount(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "get_account",
    {
      title: "Get an account",
      description: `Returns a single chart-of-accounts entry by id, organization-scoped (FORCE RLS). Returns 404 when the account does not exist within the authenticated tenant — cross-tenant existence is never leaked.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("get"),
        ...getAnnotations("getAccount"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const raw = args as Record<string, unknown>
        const params = { path: { "accountId": raw["accountId"] } } as unknown as NonNullable<operations["getAccount"]["parameters"]>
        const init = { params }
        const { data, error, response } = await client.GET("/v1/accounts/{accountId}", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
