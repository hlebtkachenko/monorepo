---
category: Added
---

Wire the OAuth 2.1 resource-server env (`OAUTH_ISSUER`/`OAUTH_JWKS_URI`/`OAUTH_RESOURCE`) into the API container so deployed environments accept OAuth access tokens on `/v1/*` alongside API keys.
