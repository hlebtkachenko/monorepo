// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, operations } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "entityType": z.enum(["EVENT","DOCUMENT","ASSET","INVENTORY_COUNT"]).describe("Filter by entity type.").optional(),
}

export function registerListAccountingNumberSeries(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "list_accounting_number_series",
    {
      title: "List number series",
      description: `Returns the organization's gapless number series (optionally filtered by entity type). Write bodies reference a series by \`seriesId\`; this is how an agent discovers those ids. Organization-scoped (FORCE RLS).`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("get"),
        ...getAnnotations("listAccountingNumberSeries"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const raw = args as Record<string, unknown>
        const params = { query: { "entityType": raw["entityType"] } } as unknown as NonNullable<operations["listAccountingNumberSeries"]["parameters"]>
        const init = { params }
        const { data, error, response } = await client.GET("/v1/accounting/number-series", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
