---
category: Changed
---

Debug Archetype Table reference (#877): selection Export is now a segmented Copy-to-clipboard / Export-as-CSV button group built by a shared buildTableFooter so both the Normal and Pivot tables get it; the row Inspector wires real Attachments (preview/download/rename/copy) and validated link attachments with an open-external redirect, plus a working Export tab and a More tab (Duplicate/Archive/Delete); the toolbar search keeps a fixed width, Inspector sections use a 32px rhythm, and the rail drops the never-shipped Settings tab in favour of More.
