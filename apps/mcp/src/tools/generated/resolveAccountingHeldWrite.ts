// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, components, operations } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "action": z.enum(["approve","reject"]).describe("approve = execute the stored payload through the original domain path; reject = close the review without any domain write."),
  "note": z.string().max(1000).describe("Reviewer note, persisted to the audit trail.").optional(),
  "id": z.string().describe("Held-write id (the reviewId returned by the held write). Resolved within the API key's own organization (FORCE RLS)."),
}

export function registerResolveAccountingHeldWrite(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "resolve_accounting_held_write",
    {
      title: "Resolve a held write",
      description: `Approve or reject a held write. Approve executes the stored payload through the same domain path the original endpoint would have used, with the approver as the responsible user; reject closes the review with no domain write. The row id is the idempotency anchor — no Idempotency-Key header; a second resolve returns 409.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("post"),
        ...getAnnotations("resolveAccountingHeldWrite"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const raw = args as Record<string, unknown>
        const params = { path: { "id": raw["id"] } } as unknown as NonNullable<operations["resolveAccountingHeldWrite"]["parameters"]>
        const paramKeys = new Set(["id"])
        const bodyFields = Object.fromEntries(Object.entries(raw).filter(([k]) => !paramKeys.has(k)))
        const body = bodyFields as unknown as components["schemas"]["ResolveHeldWriteRequest"]
        const init = { body, params }
        const { data, error, response } = await client.POST("/v1/accounting/held-writes/{id}/resolve", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
