# `.brain/judge/` — judge calibration

The judge rubric + reliability tracking for the Advisor/judge layer. `calibration.md` (Cohen's kappa
≥ 0.80 vs Hleb, M2+ only) lands once Hleb-vs-Advisor pairs exist (post-M2). The judge rubric is a
risky class — human-gated. Empty at M0.
