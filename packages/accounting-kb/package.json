{
  "name": "@workspace/accounting-kb",
  "version": "0.0.1",
  "private": true,
  "description": "Vendored machine-readable subset of the Czech accounting knowledge base (decision trees, předkontace, chart of accounts, Q-pattern index). Versioned via version.json; kb_version is recorded per Brain eval run. Consumed source-first by @workspace/brain.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint",
    "test": "vitest run",
    "test:watch": "vitest",
    "vendor": "node scripts/vendor.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "eslint": "^9.39.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
