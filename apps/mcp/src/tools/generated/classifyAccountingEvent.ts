// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, components } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "direction": z.enum(["RECEIVED","ISSUED"]).describe("FP (RECEIVED / purchase) vs FV (ISSUED / sale)."),
  "supplyKind": z.enum(["GOODS","MATERIAL","SERVICES","UTILITY","RENT","INSURANCE","ASSET","ADVANCE","CREDIT_NOTE","OTHER"]).describe("Kind of supply."),
  "jurisdiction": z.enum(["DOMESTIC","REVERSE_CHARGE","EU","IMPORT","EXEMPT","OUTSIDE_VAT"]).describe("VAT jurisdiction."),
  "base": z.string().describe("Unsigned decimal amount as a string."),
  "vat": z.string().describe("Unsigned decimal amount as a string."),
  "vatRate": z.string().nullable().describe("Stated VAT rate.").optional(),
  "currency": z.string().min(3).max(3).describe("ISO 4217."),
  "fxRate": z.string().nullable().describe("FX rate if foreign currency.").optional(),
  "serviceWindow": z.object({ "start": z.string(), "end": z.string() }).describe("Service window (ISO dates) — deferral split.").optional(),
  "periodEnd": z.string().describe("Accounting period end (ISO).").optional(),
  "durable": z.boolean().describe("Durable long-term asset?").optional(),
  "assetThreshold": z.string().describe("Unsigned decimal amount as a string.").optional(),
  "acquisitionAccount": z.string().min(3).max(6).describe("042/041 for a capitalised asset.").optional(),
  "isCreditNote": z.boolean().describe("Credit note (§42) — flips sides.").optional(),
}

export function registerClassifyAccountingEvent(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "classify_accounting_event",
    {
      title: "Classify an economic event",
      description: `Pure decision: given raw economic-event facts, returns the accounting treatment (VAT mode, předkontace scenario, capitalisation/deferral, open-item account) with a law-cited reasoning trail. No mutation, no tenant read — safe and repeatable.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("post"),
        ...getAnnotations("classifyAccountingEvent"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const body = args as unknown as components["schemas"]["ClassifyEventRequest"]
        const { data, error, response } = await client.POST("/v1/accounting/classify", { body })
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
