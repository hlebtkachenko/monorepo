---
category: Added
---

Let hosted MCP clients select a tool subset at connect time via `?groups=` / `?scope=` on the Worker URL, expose the group catalog at `GET /groups`, and always register a `list_tool_groups` discovery tool.
