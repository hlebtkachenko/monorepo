---
category: Fixed
---

Render the app-shell header and rail inside the shared tooltip provider so Tooltip-based header/rail children (org switcher, header menu, collapsed rail icons) no longer throw "Tooltip must be used within TooltipProvider".
