---
category: Changed
---

Per-filing dispatch (title, version, druh podání and its evidence field) lives in one DPH_KINDS descriptor instead of duplicated tables and a ternary repeated across two modules.
