---
category: Changed
---

Made the org-slug route segment injectable in the app-context-menu `guessPageFile` helper (`orgSlugSegment`, default `[orgSlug]`) so the `@workspace/ui` block no longer hardcodes the consuming app's route-tree name (orgSlug Track A).
