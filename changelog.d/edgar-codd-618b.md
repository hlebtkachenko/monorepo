---
category: Added
---

Add `docs/runbooks/BETA-DEPLOY.md`, the first-deploy gate checklist for the beta environment: GitHub `beta` environment + secrets, the least-privilege AWS deploy role, the beta Cloudflare tunnel and DNS record, Vault/SSM secret seeding, the local `AWS_PROFILE=hleb` fallback deploy, and the forward-fix-only rollback path.
