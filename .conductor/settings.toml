"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "pnpm install --frozen-lockfile && pnpm codegraph:ready"

[scripts.run.codegraph]
command = "pnpm codegraph:ready"
icon = "network"
