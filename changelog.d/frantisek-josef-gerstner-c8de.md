---
category: Fixed
---

Restored page scroll in the beta portal: every page body now lives in the shell's own scroll region, so content past the fold is reachable instead of clipped by the app-shell's non-scrolling body slot.
