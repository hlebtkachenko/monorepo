---
category: Changed
---

Completed the beta cross-org isolation suite for migrations 0016-0019: the six WHERE-clause conjuncts and composite FKs that no assertion stood on — `payrollLinesForPeriod` and `publishedPayrollPeriods`, the organization arm of the chat writes (isolated by putting one person in two books), `chat_message_chat_fk`, the `chat_usage` identity freeze, and `payroll_summary_batch_fk`.
