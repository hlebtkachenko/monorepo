---
category: Fixed
---

The DPPO writer now sorts the přílohy věty onto the XSD sequence itself instead of trusting each builder's push order, normalizes the rozvahový den in both places it lands, and warns when a return carries a výkaz without the rozvahový den or the unit that EPO's kritická kontrola then rejects.
