// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, operations } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "id": z.string().uuid().describe("OCR template id, resolved within the API key's workspace."),
}

export function registerConfirmOcrTemplate(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "confirm_ocr_template",
    {
      title: "Confirm an OCR extraction template",
      description: `Marks a template as human-confirmed (sets \`humanConfirmedAt\` to now). HUMAN-ACTOR ONLY: an agent-actor key is rejected with 403 — confirmation is the trust boundary that a human, not the Brain, must cross. Returns 404 when the template is not visible to the caller's workspace.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("post"),
        ...getAnnotations("confirmOcrTemplate"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const raw = args as Record<string, unknown>
        const params = { path: { "id": raw["id"] } } as unknown as NonNullable<operations["confirmOcrTemplate"]["parameters"]>
        const init = { params }
        const { data, error, response } = await client.POST("/v1/ocr-templates/{id}/confirm", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
