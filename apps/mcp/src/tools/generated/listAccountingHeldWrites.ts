// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import type { AfframeClient } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

export function registerListAccountingHeldWrites(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "list_accounting_held_writes",
    {
      title: "List held writes",
      description: `Returns the organization's review queue — gated writes that were HELD (below the confidence threshold or above the always-hold amount) and await a human decision, oldest first. Organization-scoped (FORCE RLS).`,
      annotations: {
        ...defaultAnnotationsForMethod("get"),
        ...getAnnotations("listAccountingHeldWrites"),
      },
    },
    async (): Promise<CallToolResult> => {
      try {
        const { data, error, response } = await client.GET("/v1/accounting/held-writes")
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
