// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, components } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "entityType": z.enum(["EVENT","DOCUMENT","ASSET","INVENTORY_COUNT"]).describe("What the series numbers."),
  "code": z.string().min(1).max(32).describe("Company série label (unique per entity type)."),
  "pattern": z.string().min(1).max(64).describe("Designation format. `{YYYY}` = year, `{NNNN}` = zero-padded sequence."),
  "nextNumber": z.number().int().min(1).describe("First sequence number to allocate (defaults to 1).").optional(),
}

export function registerCreateNumberSeries(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "create_number_series",
    {
      title: "Create a number series",
      description: `Creates a gapless číselná řada for the organization. Onboarding tool: an org needs a DOCUMENT/EVENT series before it can book. Tenant injected from the API-key principal. Requires the \`accounting:write\` scope.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("post"),
        ...getAnnotations("createNumberSeries"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const body = args as unknown as components["schemas"]["CreateNumberSeriesRequest"]
        const init = { body }
        const { data, error, response } = await client.POST("/v1/accounting/number-series", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
