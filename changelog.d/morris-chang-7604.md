---
category: Fixed
---

Anchored the beta SF-3 `app_user` write-fence exemptions to real paths instead of directory basenames, and widened its raw-SQL pattern to catch schema-qualified, `ONLY` and line-broken spellings of an `app_user` write.
