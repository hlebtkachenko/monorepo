// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, components } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "counterpartyKey": z.string().min(1).max(255).describe("Counterparty identity — an IČO or normalized name."),
  "direction": z.enum(["RECEIVED","ISSUED"]).describe("FP (RECEIVED) vs FV (ISSUED)."),
  "supplyKind": z.enum(["GOODS","MATERIAL","SERVICES","UTILITY","RENT","INSURANCE","ASSET","ADVANCE","CREDIT_NOTE","OTHER"]).describe("Kind of supply."),
  "jurisdiction": z.enum(["DOMESTIC","REVERSE_CHARGE","EU","IMPORT","EXEMPT","OUTSIDE_VAT"]).describe("VAT jurisdiction."),
  "confirmedDecision": z.object({ "vatMode": z.enum(["STANDARD","REVERSE_CHARGE","EXEMPT","OUTSIDE_VAT","IMPORT"]).describe("VAT mode to stamp on the partial record."), "vatJurisdiction": z.enum(["DOMESTIC","REVERSE_CHARGE","EU","IMPORT","EXEMPT","OUTSIDE_VAT"]).describe("vat_jurisdiction to stamp on the capture partial — splits an EU supply (ř.20/21 + Souhrnné hlášení) from a domestic §92 PDP (ř.25 + KH A.1)."), "vatRate": z.string().nullable().describe("Rate to freeze (null for exempt/outside)."), "scenario": z.string().describe("Předkontace scenario id."), "accountOverrides": z.record(z.string(), z.unknown()).describe("Template→tenant account remap.").optional(), "saldoAccount": z.enum(["311","321"]).nullable().describe("Open-item account, or null."), "capitalise": z.object({ "acquisitionAccount": z.string() }).describe("Route net to an acquisition account.").optional(), "deferral": z.object({ "bridge": z.enum(["381","384"]), "reason": z.string() }).describe("Defer the future part to a bridge account.").optional(), "commodityCode": z.enum(["1","3","4","5"]).nullable().describe("§92 kód předmětu plnění to stamp on the partial record for kontrolní hlášení A.1/B.1; null unless this is a domestic reverse-charge supply."), "reasoning": z.array(z.string()).describe("Law-cited decision trail.") }).describe("The accounting treatment decided from the facts (with reasoning)."),
  "provenance": z.record(z.string(), z.unknown()).nullable().describe("Opaque provenance blob.").optional(),
}

export function registerCreateBookingTemplate(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "create_booking_template",
    {
      title: "Create a booking template",
      description: `Creates a NEW, UNCONFIRMED booking template in the caller's workspace. The server pins \`humanConfirmedAt\` to null and \`matchCount\`/\`heldCount\` to 0 — a fresh template is never auto-trusted and NEVER matchable until a human confirms it via \`POST /v1/booking-templates/{id}/confirm\`. Agent keys may create (the DRAFT itself carries no write authority). Requires the \`accounting:write\` scope. The workspace comes from the API key.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("post"),
        ...getAnnotations("createBookingTemplate"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const body = args as unknown as components["schemas"]["CreateBookingTemplateRequest"]
        const init = { body }
        const { data, error, response } = await client.POST("/v1/booking-templates", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
