---
category: Added
bump: minor
---

Brain: the extract vision model emits a document-grounded `supply_kind` on the IR Invoice, threaded through the capture adapter so an approved invoice books to the right cost account instead of holding on a null; an absent or unrecognized value fails safe to a hold.
