// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, operations } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "invoiceId": z.string().describe("Opaque invoice identifier (UUID)."),
}

export function registerGetInvoice(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "get_invoice",
    {
      title: "Get an invoice",
      description: `Returns a single invoice with its individual-record lines and partial-record money decomposition, organization-scoped (FORCE RLS). Returns 404 when the invoice is not visible to the authenticated tenant.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("get"),
        ...getAnnotations("getInvoice"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const raw = args as Record<string, unknown>
        const params = { path: { "invoiceId": raw["invoiceId"] } } as unknown as NonNullable<operations["getInvoice"]["parameters"]>
        const init = { params }
        const { data, error, response } = await client.GET("/v1/invoices/{invoiceId}", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
