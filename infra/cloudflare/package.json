{
  "name": "@workspace/cloudflare-turbo-cache",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers + R2 backend for Turborepo Remote Cache. Vendored from AdiRishi/turborepo-remote-cache-cloudflare v4.0.0 — see SOURCE.md.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/valibot-validator": "^0.6.1",
    "hono": "^4.12.25",
    "valibot": "^1.4.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260713.1",
    "@workspace/typescript-config": "workspace:*",
    "typescript": "^7.0.2",
    "wrangler": "^4.110.0"
  }
}
