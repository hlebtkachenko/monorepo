---
category: Fixed
---

The workbook reader now handles a real hand-built účetní deník: tabs prefixed with an index and company code, a title band above the header row, no POHODA TpUD column, and several companies in one file (every match is named in a warning instead of the first being taken silently).
