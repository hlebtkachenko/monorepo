// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, operations } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "id": z.string().uuid().describe("Booking template id, resolved within the API key's workspace."),
}

export function registerConfirmBookingTemplate(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "confirm_booking_template",
    {
      title: "Confirm a booking template",
      description: `Marks a booking template as human-confirmed (sets \`humanConfirmedAt\` to now) — the ONLY way a template becomes matchable. HUMAN-ACTOR ONLY: an agent-actor key is rejected with 403 — confirmation is the trust boundary that a human, not the Brain, must cross (§I9 amendment: a booking template is created only from a human-confirmed booking treatment). Returns 404 when the template is not visible to the caller's workspace, and 409 when the workspace already has a CONFIRMED template for the same signature.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("post"),
        ...getAnnotations("confirmBookingTemplate"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const raw = args as Record<string, unknown>
        const params = { path: { "id": raw["id"] } } as unknown as NonNullable<operations["confirmBookingTemplate"]["parameters"]>
        const init = { params }
        const { data, error, response } = await client.POST("/v1/booking-templates/{id}/confirm", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
