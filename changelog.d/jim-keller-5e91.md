---
category: Fixed
---

Correct the beta-agent CLI's stale saldokonto/payroll endpoint pointers: both cited the wrong campaign PR numbers even when written, and both server routes are live now — the CLI's refusal message and README/datasets.ts docs say so instead of naming a PR.
