# `.brain/memory/` — durable cross-run memory

Distilled, durable notes the Brain carries between runs (org-specific quirks, recurring patterns,
resolved ambiguities). Distinct from a run's transient working memory. Single-writer (librarian) via
GitHub PR — never a parallel-worktree local write. Empty at M0.
