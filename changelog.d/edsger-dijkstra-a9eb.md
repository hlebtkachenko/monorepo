---
category: Fixed
---

Closed four gaps in the beta employee-seat boundary walker: nested and dynamic route leaves under a page-gated module, every leaf inside an allowlisted module, an AST check for `payrollScope(scope)` in place of a substring match that prose satisfied, and a registration fence over every `app/api/orgs/[orgSlug]` route handler — and derived its failure message from the gate list it describes, so the diagnostic can no longer name fewer gates than the fence accepts.
