---
category: Fixed
---

Printed výkazy paginate on measured row heights instead of CSS page-break hints, so a row no longer splits across the fold and every page repeats the column header in Safari as well as Chrome.
