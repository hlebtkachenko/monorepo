// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, components } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "type": z.enum(["bug","request","issue","question"]).describe("Feedback category. `bug` = something broken; `request` = new feature ask; `issue` = process / UX / docs problem; `question` = support question that didn't fit the FAQ."),
  "message": z.string().min(1).max(4000).describe("Free-form feedback text. 1–4000 characters. Markdown allowed but not rendered — the Linear issue treats this as plain text."),
  "email": z.string().max(254).describe("Optional contact email. If provided, support will reply here; otherwise the response is filed without a return path. Already-authenticated keys carry an org contact so this is purely for users who want a direct reply.").optional(),
  "context": z.object({ "page": z.object({ "url": z.string().max(2048), "pathname": z.string().max(512), "title": z.string().max(500).nullable().optional(), "locale": z.string().max(16).nullable().optional(), "theme": z.enum(["light","dark","system"]).nullable().optional(), "referrer": z.string().max(2048).nullable().optional() }).describe("Page the report was filed from.").optional(), "scope": z.object({ "org_slug": z.string().max(64).optional(), "reporter_id": z.string().max(64).optional(), "reporter_email": z.string().max(320).optional() }).describe("App scope + server-resolved reporter identity. Advisory only — never used for authorization.").optional(), "element": z.object({ "tag": z.string().max(64), "data_slot": z.string().max(128).nullable().optional(), "role": z.string().max(64).nullable().optional(), "id": z.string().max(128).nullable().optional(), "classes": z.string().max(500).nullable().optional(), "text": z.string().max(1000).optional(), "dom_path": z.string().max(2000).optional(), "bounding_rect": z.object({ "top": z.number(), "left": z.number(), "width": z.number(), "height": z.number() }).nullable().optional() }).describe("Right-clicked element descriptor.").optional(), "selection": z.object({ "text": z.string().max(4000).nullable().optional(), "html": z.string().max(4000).nullable().optional(), "rect": z.object({ "top": z.number(), "left": z.number(), "width": z.number(), "height": z.number() }).nullable().optional() }).describe("Active text selection, if any.").optional(), "surrounding": z.object({ "nearest_heading": z.string().max(300).nullable().optional(), "inferred_block": z.string().max(128).nullable().optional(), "nearby_text": z.string().max(2000).optional() }).describe("Nearby DOM text for locating the report.").optional(), "viewport": z.object({ "width": z.number().int().min(0).max(20000), "height": z.number().int().min(0).max(20000), "scroll_y": z.number().int().min(0).max(1000000), "device_pixel_ratio": z.number().min(0).max(8) }).describe("Viewport dimensions at capture time.").optional(), "client": z.object({ "user_agent": z.string().max(800), "platform": z.string().max(128).nullable().optional(), "language": z.string().max(32).nullable().optional(), "timezone": z.string().max(64).nullable().optional(), "online": z.boolean().optional(), "prefers_dark": z.boolean().optional() }).describe("Reporter's browser/runtime environment.").optional() }).describe("Optional in-app capture context (page, element, viewport, client). Public callers omit it; the in-app reporter attaches it. Folded into the Linear issue when present.").optional(),
}

export function registerCreateFeedback(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "create_feedback",
    {
      title: "Send feedback",
      description: `Submit a bug report, feature request, process issue, or question. The api forwards every submission to \`support+feedback@afframe.com\` and creates a Linear issue tagged with the feedback type. Public — no API key required.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("post"),
        ...getAnnotations("createFeedback"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const body = args as unknown as components["schemas"]["CreateFeedbackRequest"]
        const { data, error, response } = await client.POST("/v1/feedback", { body })
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
