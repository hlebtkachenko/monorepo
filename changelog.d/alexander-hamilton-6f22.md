---
category: Added
bump: minor
---

Add the `fx_rate` (shared ČNB reference) and `fx_rate_override` (org-scoped, FORCE RLS) tables for the Finance module — the multi-currency rate store with precedence org override → ČNB → error.
