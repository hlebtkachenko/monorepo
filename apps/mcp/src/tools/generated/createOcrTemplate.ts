// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, components } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "supplierKey": z.string().min(1).max(255).describe("Supplier identity — an IČO or normalized supplier name."),
  "docKind": z.string().min(1).max(255).describe("Document kind this template extracts."),
  "locators": z.record(z.string(), z.unknown()).describe("Field → region map (opaque Brain-owned JSON)."),
  "layoutFingerprint": z.string().max(255).nullable().describe("Hash of the field-region geometry (drift detection).").optional(),
  "provenance": z.record(z.string(), z.unknown()).nullable().describe("Opaque provenance blob.").optional(),
}

export function registerCreateOcrTemplate(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "create_ocr_template",
    {
      title: "Create an OCR extraction template",
      description: `Creates a NEW, UNCONFIRMED template in the caller's workspace. The server pins \`humanConfirmedAt\` to null and \`heldCount\` to 0 — a fresh template is never auto-trusted; a human must confirm it via \`POST /v1/ocr-templates/{id}/confirm\`. Agent keys may create. Requires the \`accounting:write\` scope. The workspace comes from the API key.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("post"),
        ...getAnnotations("createOcrTemplate"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const body = args as unknown as components["schemas"]["CreateOcrTemplateRequest"]
        const init = { body }
        const { data, error, response } = await client.POST("/v1/ocr-templates", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
