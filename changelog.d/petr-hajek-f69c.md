---
category: Removed
---

Removed the unrequested `/o` settings stub page and every link to it (the profile-menu Settings item, plus the org switcher's Settings and Invite buttons that both pointed at the nonexistent settings route); also deleted the now-unused bespoke FavoritePageHeader, superseded by the shared ContentHeader favorite.
