---
category: Changed
---

CI: parallelize the required gate — shard Storybook a11y/interaction tests 4 ways, split typecheck, ESLint, and apps/web integration tests into their own jobs, drop the unused webkit browser, and scope PR builds to `--affected` (main/release builds stay full-graph).
