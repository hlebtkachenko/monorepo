// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, operations } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "counterpartyKey": z.string().min(1).max(255).describe("Filter to one counterparty.").optional(),
}

export function registerListBookingTemplates(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "list_booking_templates",
    {
      title: "List booking templates",
      description: `Returns the workspace's booking templates — the Brain's reviewable, human-confirmed recurring-case treatments (M2.1), WORKSPACE-scoped (FORCE RLS) and shared across every organization in the accountant's office. Optionally filter by \`counterpartyKey\`. Both human and agent keys may read.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("get"),
        ...getAnnotations("listBookingTemplates"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const raw = args as Record<string, unknown>
        const params = { query: { "counterpartyKey": raw["counterpartyKey"] } } as unknown as NonNullable<operations["listBookingTemplates"]["parameters"]>
        const init = { params }
        const { data, error, response } = await client.GET("/v1/booking-templates", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
