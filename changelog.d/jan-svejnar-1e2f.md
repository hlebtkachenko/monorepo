---
category: Fixed
---

A doklad split across two sazby no longer lets the first row inherit the whole doklad's total, and switching the DPH storage mode no longer deletes the only copy when the save fails.
