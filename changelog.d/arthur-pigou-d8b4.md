---
category: Changed
---

CI: enable CodeQL TRAP caching so PR runs re-extract only changed source files instead of re-parsing the whole monorepo each time — same database and security-extended queries, faster extraction phase.
