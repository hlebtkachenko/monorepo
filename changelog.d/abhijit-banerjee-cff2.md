---
category: Added
bump: minor
---

`closePeriod` — year-end close: result-close (710→431) then the 702 (Konečný účet rozvažný) balance-close tagged is_closing with a KÚR balance-equation assert, účetní závěrka output, seal, and carryover (find-or-create the next period, post the 701 opening once with a net-balance double-open guard). `rollForwardPeriod` is now a thin wrapper over it.
