---
category: Added
---

The Debug reference Table gained a real per-row Activity log (each edit logged with a working Undo), an Export dropdown (Copy to clipboard / CSV of the selected rows x visible columns in order), a Delete with Undo, per-row Attachments and a Settings tab, and a shareable ?inspect= deep-link; the Pivot page gained its search box.
