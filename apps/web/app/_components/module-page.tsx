/**
 * Re-exported from the org tier's copy — both tiers use the identical
 * placeholder shape, and this file is a cross-tier-shared location
 * (`workspace-shell.tsx` and other tier-agnostic components already live
 * here). No workspace-specific behavior to diverge on.
 */
export { ModulePage } from "../[orgSlug]/_components/module-page"
