// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, operations } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "includeDeleted": z.enum(["true","false"]).describe("When 'true', include soft-deleted documents (still within the 60-day redemption window). Defaults to excluding them.").optional(),
}

export function registerListDocuments(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "list_documents",
    {
      title: "List documents",
      description: `Returns the caller's workspace documents (confirmed source-document blobs in the S3 document store). WORKSPACE-scoped (FORCE RLS). By default soft-deleted documents are excluded; pass \`includeDeleted=true\` to include those still inside the 60-day redemption window. The workspace comes from the API key; no tenant identifiers are accepted.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("get"),
        ...getAnnotations("listDocuments"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const raw = args as Record<string, unknown>
        const params = { query: { "includeDeleted": raw["includeDeleted"] } } as unknown as NonNullable<operations["listDocuments"]["parameters"]>
        const init = { params }
        const { data, error, response } = await client.GET("/v1/documents", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
