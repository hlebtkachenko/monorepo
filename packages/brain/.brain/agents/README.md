# `.brain/agents/` — Brain runtime subagent definitions

Markdown definitions for the Brain's RUNTIME subagents (parser, classifier, verifier, advisor,
librarian) — distinct from the BUILD subagents. Authored by the Plan agent and **advisor-gated**
(§2.3). Each pins its model tier, tools, and the constitution invariants it must honor. Empty at M0;
authored alongside the orchestrator (WP-1.4b).
