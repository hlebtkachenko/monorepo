---
category: Added
bump: minor
---

ContentHeader block gains a self-managing favorite star (`useOptimisticFavorite` + `ContentHeaderFavoriteToggle`) that the Blank, Table, and Details archetypes forward; OrgSwitcher `settingsHref`/`inviteHref` are now optional so a surface with no settings/invite page hides those buttons.
