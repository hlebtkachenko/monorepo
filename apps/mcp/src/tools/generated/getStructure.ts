// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import type { AfframeClient } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

export function registerGetStructure(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "get_structure",
    {
      title: "Get application structure",
      description: `Returns the org application's information architecture — the ten rail modules, each with its full page tree (pages → subpages), build-status, and layout archetype. Start here to discover what the app exposes without driving a browser. Public — no API key required; the structure is tenant-agnostic.`,
      annotations: {
        ...defaultAnnotationsForMethod("get"),
        ...getAnnotations("getStructure"),
      },
    },
    async (): Promise<CallToolResult> => {
      try {
        const { data, error, response } = await client.GET("/v1/structure")
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
