// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, operations } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "periodId": z.string().describe("Accounting period id to read. Resolved within the API key's own organization (FORCE RLS); a period from another tenant returns 404."),
  "rozsah": z.enum(["FULL","ABBREVIATED"]).describe("Plný / zkrácený rozsah.").optional(),
  "unit": z.enum(["CZK","THOUSANDS"]).describe("Presentation unit (celé Kč / v tisících).").optional(),
}

export function registerGetAccountingStatementLayout(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "get_accounting_statement_layout",
    {
      title: "Get formatted statement layout (rozvaha / VZZ)",
      description: `Formatted rozvaha + výkaz zisku a ztráty per Decree 500/2002 přílohy (plný / zkrácený rozsah; celé Kč / v tisících). Organization-scoped.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("get"),
        ...getAnnotations("getAccountingStatementLayout"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const raw = args as Record<string, unknown>
        const params = { path: { "periodId": raw["periodId"] }, query: { "rozsah": raw["rozsah"], "unit": raw["unit"] } } as unknown as NonNullable<operations["getAccountingStatementLayout"]["parameters"]>
        const init = { params }
        const { data, error, response } = await client.GET("/v1/accounting/periods/{periodId}/outputs/statement-layout", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
