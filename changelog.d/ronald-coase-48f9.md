---
category: Added
---

Added Debug "Archetype Table" reference pages in the new `/o/[orgSlug]` tree — Normal Table (views, favorite, auto-generated per-column filter, selection footer, and a multi-tab row Inspector) and Pivot Table — the first governed consumers of the section-library policy, wiring `ArchetypeTable` + the Table/Pivot Body + Inspector entirely from the packages/ui blocks. They read dedicated dev-seeded `demo_debug_*` tables (never real product data; empty in prod), so the pages double as clone-ready templates.
