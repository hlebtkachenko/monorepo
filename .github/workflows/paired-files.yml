name: Paired files

on:
  pull_request:
    branches: [main, main-beta]

permissions: {}

concurrency:
  group: paired-files-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  check:
    # beta fast lane (Hleb, 2026-08-27): changelog/governance checks skip on
    # main-beta PRs unless the `ci-full` label is present. `main-beta` has no
    # required-status-check ruleset of its own (only `main.json` exists), so
    # this job simply not reporting on a main-beta PR does not strand it.
    # See the policy block atop ci.yml.
    if: >-
      github.base_ref != 'main-beta' || contains(github.event.pull_request.labels.*.name, 'ci-full')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Check paired files
        run: bash .github/scripts/check-paired-files.sh
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
      - name: Check documentation
        run: node scripts/check-docs.mjs
      - name: Setup pnpm + Node + install
        uses: ./.github/actions/setup
      - name: Check archetype body ratchet
        run: node scripts/governance/check-archetype-body.mjs
      - name: Check changelog fragment added
        if: ${{ github.event.pull_request.user.login != 'dependabot[bot]' }}
        run: node scripts/governance/check-changelog-fragment.mjs
        env:
          BASE_REF: ${{ github.event.pull_request.base.sha }}
          HEAD_REF: ${{ github.event.pull_request.head.sha }}
          PR_TITLE: ${{ github.event.pull_request.title }}
