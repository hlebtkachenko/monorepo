---
category: Changed
---

Make @afframe/mcp publishable to npm: drop `private`, bump to 0.1.0, ship only `bin` + the bundled `dist/server.js` (no runtime dependencies), add `publishConfig`/`repository`/`engines` and a bundled LICENSE.
