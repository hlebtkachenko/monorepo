---
category: Added
---

Add FX rate resolution (`resolveFxRate`) and conversion (`convertAmount`/`convertAmountAt`) over the new rate store — the fetch half of the FX story (override → ČNB → error, never inverted, never a neighbour date), computed in SQL.
