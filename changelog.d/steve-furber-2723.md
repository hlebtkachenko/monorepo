---
category: Added
bump: minor
---

Chart of accounts rows open a read-only Inspector rail with the account's details; the archetype row-rail now resolves tree-table sections (nested rows), skips structural tier nodes, and expands ancestors on deep-link.
