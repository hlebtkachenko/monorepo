// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, components, operations } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "periodId": z.string().describe("Účetní období."),
  "seriesId": z.string().describe("EVENT number series (see GET number-series)."),
  "partyId": z.string().nullable().describe("OUR side (counterparty row); null for internal.").optional(),
  "counterpartyId": z.string().nullable().describe("THEIR side (counterparty row).").optional(),
  "description": z.string().min(1).max(2000).describe("Case description."),
  "content": z.string().max(10000).nullable().describe("Optional detail.").optional(),
  "occurredAt": z.string().describe("Okamžik uskutečnění (§11/1e) — ISO date/datetime in the period."),
  "confidence": z.number().min(0).max(1).describe("Agent's confidence [0,1]. Writes at/above the server threshold auto-apply; below it are HELD for human review. Required."),
  "rationale": z.string().min(1).max(2000).describe("Why this write — persisted to the audit trail. Required."),
  "conversationId": z.string().describe("Audit-correlation id of the driving agent conversation.").optional(),
  "idempotency-key": z.string().describe("Idempotency key (1–255 chars); reuse on retry."),
}

export function registerCreateAccountingEvent(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "create_accounting_event",
    {
      title: "Create an accounting event",
      description: `Create an účetní případ. Gated: auto-applies (201) at/above the confidence threshold, otherwise held (202) for human review. Tenant + responsible user injected from the API-key principal.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("post"),
        ...getAnnotations("createAccountingEvent"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const raw = args as Record<string, unknown>
        const params = { header: { "idempotency-key": raw["idempotency-key"] } } as unknown as NonNullable<operations["createAccountingEvent"]["parameters"]>
        const paramKeys = new Set(["idempotency-key"])
        const bodyFields = Object.fromEntries(Object.entries(raw).filter(([k]) => !paramKeys.has(k)))
        const body = bodyFields as unknown as components["schemas"]["CreateAccountingEventRequest"]
        const init = { body, params }
        const { data, error, response } = await client.POST("/v1/accounting/events", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
