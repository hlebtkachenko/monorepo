// AUTO-GENERATED by apps/mcp/scripts/gen-tools.ts — do not edit.
import { z } from "zod"
import type { AfframeClient, components } from "@afframe/sdk"
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import type { CallToolResult } from "@modelcontextprotocol/sdk/types.js"
import { renderResult, toolError } from "../_render"
import { defaultAnnotationsForMethod, getAnnotations } from "../_curate"

const inputShape = {
  "periodStart": z.string().describe("First day of the účetní období (YYYY-MM-DD)."),
  "periodEnd": z.string().describe("Last day of the účetní období. Omit to derive the fiscal-year end containing periodStart from the org's fiscal-year start month.").optional(),
  "regimeCode": z.enum(["DOUBLE_ENTRY","SINGLE_ENTRY","TAX_RECORDS"]).describe("Bookkeeping regime. Required only when the org's legal form permits more than one and no period exists yet; otherwise derived/reused.").optional(),
  "accountingCurrency": z.string().min(3).max(3).describe("Měna účetnictví (ISO 4217). Defaults to CZK.").optional(),
  "accountingSizeCode": z.enum(["MICRO","SMALL","MEDIUM","LARGE"]).nullable().describe("Účetní jednotka size category (§1b). Null until assessed.").optional(),
  "fxRatePolicy": z.enum(["DAILY","REAL","FIXED"]).nullable().describe("FX-rate směrnice policy (§24). Null defaults to DAILY.").optional(),
}

export function registerCreateAccountingPeriod(
  server: McpServer,
  client: AfframeClient,
): void {
  server.registerTool(
    "create_accounting_period",
    {
      title: "Open an accounting period",
      description: `Opens an účetní období and its COUPLED scaffold — chart of accounts (double-entry) + default number series — so the org is fully bookable. The regime is reused from an existing period or derived from the legal form. Tenant injected from the principal. Requires \`accounting:write\`.`,
      inputSchema: inputShape,
      annotations: {
        ...defaultAnnotationsForMethod("post"),
        ...getAnnotations("createAccountingPeriod"),
      },
    },
    async (args): Promise<CallToolResult> => {
      try {
        const body = args as unknown as components["schemas"]["CreateAccountingPeriodRequest"]
        const init = { body }
        const { data, error, response } = await client.POST("/v1/accounting/periods", init)
        if (error) throw error
        if (!response.ok) {
          throw new Error(`Upstream HTTP ${response.status}`)
        }
        return renderResult(data)
      } catch (err) {
        return toolError(err)
      }
    },
  )
}
