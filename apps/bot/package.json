{
  "name": "@workspace/bot",
  "version": "0.0.0",
  "private": true,
  "description": "Afframe Telegram bot — sole owner of all Telegram I/O. grammY on a Cloudflare Worker. Outbound /ingest + inbound /webhook.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@workspace/notify": "workspace:*",
    "grammy": "^1.44.0",
    "hono": "^4.12.25"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "@workspace/typescript-config": "workspace:*",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.110.0"
  }
}
