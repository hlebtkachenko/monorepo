---
category: Fixed
---

Hosted MCP OAuth: accept the trailing-slash resource audience (`https://mcp.afframe.com/`) that MCP clients derive from the registered server URL, so the token exchange no longer dead-ends on `requested resource invalid`.
