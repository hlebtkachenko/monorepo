---
category: Fixed
---

Workbook join messages name the doklad as the deník spells it, instead of rebuilding it from the folded join key (which lower-cased it and split at the wrong place when a číslo contained a pipe).
