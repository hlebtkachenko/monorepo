name: shadcn component updates

on:
  schedule:
    - cron: "0 9 * * 1"
  workflow_dispatch:

permissions: {}

concurrency:
  group: shadcn-check
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup

      - name: Check for component updates
        id: diff
        run: |
          OUTPUT=$(pnpm shadcn diff 2>&1 || true)
          echo "$OUTPUT"
          if echo "$OUTPUT" | grep -qE "^[-+]"; then
            echo "has_updates=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_updates=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Ensure issue label exists
        # DEP-01: `gh issue create --label X` fails when the label does not
        # exist in the repo. Create idempotently — `|| true` swallows the
        # "already exists" error.
        if: steps.diff.outputs.has_updates == 'true'
        run: gh label create shadcn-update --color 0E8A16 --description "Upstream shadcn/ui component drift" --repo "$GITHUB_REPOSITORY" || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Check for existing issue
        if: steps.diff.outputs.has_updates == 'true'
        id: existing
        run: |
          COUNT=$(gh issue list --label shadcn-update --state open --json number --jq length)
          echo "exists=$COUNT" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create issue
        if: steps.diff.outputs.has_updates == 'true' && steps.existing.outputs.exists == '0'
        run: |
          gh issue create \
            --title "chore: shadcn/ui component updates available" \
            --label shadcn-update \
            --body "$(cat <<'BODY'
          Upstream shadcn/ui components have changes.

          Run locally to review:
          ```sh
          pnpm shadcn diff
          ```

          Update specific component:
          ```sh
          pnpm shadcn add <component-name> --overwrite
          ```
          BODY
          )"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
