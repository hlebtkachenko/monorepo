dist/
node_modules/
.next/
.turbo/
coverage/
pnpm-lock.yaml
.pnpm-store/
storybook-static/

# Agent instruction files contain literal snake_case / acc_ prefix tokens that
# prettier's markdown emphasis parser mangles (snake_case -> snake*case).
AGENTS.md
CLAUDE.md
CONTRIBUTING.md

# Generated OpenAPI spec — emitted by `pnpm --filter api emit:openapi`
# (JSON.stringify format). Prettier must not reformat it or the CI drift
# gate (openapi-lint.yml) would never match the emitter's output.
apps/api/openapi/v1.json

# Generated SDK + MCP code — emitted by `pnpm gen:all`. The drift gates
# (sdk-drift.yml, mcp-coverage.yml) re-run the same generators and compare
# against the committed output. Prettier must not reformat or the
# committed content drifts from CI's regen.
packages/sdk/src/generated/
apps/mcp/src/tools/generated/

# Vendored machine-readable KB snapshot — verbatim byte-for-byte copies of the
# source KB vault, regenerated by packages/accounting-kb/scripts/vendor.mjs and
# pinned by sha256 in version.json. Prettier must not reformat them or the
# manifest hashes (and verifyKbIntegrity()) would never match the vendored bytes.
packages/accounting-kb/coa.json
packages/accounting-kb/version.json
packages/accounting-kb/q-pattern-index.md
packages/accounting-kb/decision-trees/
packages/accounting-kb/predkontace/
