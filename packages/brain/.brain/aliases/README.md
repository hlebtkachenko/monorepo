# `.brain/aliases/` — counterparty identity aliases

Resolved `protistrana` identity links (name variants → canonical IČO/DIČ) learned across runs. Until
GATE-A1 lands IČO/DIČ on `protistrana`, dedup is name-keyed (protocol stage 2). IČO-alias resolution is
a risky class — human-gated. Empty at M0; librarian-populated via GitHub PR.
